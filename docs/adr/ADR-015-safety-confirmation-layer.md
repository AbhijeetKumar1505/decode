# ADR-015: Bind Human Approval to the Material Action

**Status:** Accepted

## Decision

Present and bind target, provider, capability/tool, material args, outputs,
credentials/data, side effects, risk, scope, and expiry to a digest. Chat text is
not an approval token.

## Consequences

Material changes need new approval. Identity/integrity/lifecycle and headless
behavior require tests/audit.
